// The panel stack's DOM contract with the frame: every layer wrapper carries
// the layer marker (e2e asserts the inert base), and the top one is the Tab
// target for the file manager.
export const DOC_LAYER_ATTR = "data-dos-doc-layer";
export const DOC_TOP_ATTR = "data-dos-doc-top";
